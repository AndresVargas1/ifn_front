import { RegisterForm } from '../components/RegisterForm';

/**
 * Page component that displays the registration form within the authentication layout.
 */
export function RegisterPage() {
  return <RegisterForm />;
}